foo: testing-purpose-service-dev

app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://127.0.0.1:5432/testing_purpose_service_db?autoReconnect=true&useSSL=false
      password: 'tsg_tps'
      pool-size: 30
      username: tsg_tps

server:
  error:
    include-binding-errors: always
    include-message: always